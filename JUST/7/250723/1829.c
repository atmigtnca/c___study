#include<stdio.h>

/*
int main(void){
    int arr[] = {1, 2, 3, 4, 5, 6};
    int* ptr = &arr[0];
    float sum = 0;

    const int LEN = sizeof(arr) / sizeof(arr[0]);

    for (int i = 0; i <LEN ; i++)
    {
        sum += *ptr++;
    }
    
    sum = sum / LEN;
    printf("%f\n", sum);

    return 0;
}
*/

int main(void){
    printf("%d", 30000*30000);
}