/*

typedef int ElementType;

Node* SLL_CreateNode(ElementType NewDate){
    Node* NewNode = (Node*)malloc(sizeof(Node));

    NewNode->Date = NewDate; //데이터를 저장한다.
    NewNode->NextNode = NULL; //다음 노드에 대한 포인터는 NULL로 초기화한다.

    return NewNode;
}

*/