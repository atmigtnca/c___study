#include<stdio.h>

int main(void){
    int a,b,v;
    scanf("%d %d %d", &a, &b, &v);

    int j;
    j = v / (a-b); // 몫
    int k;
    k = v % (a-b); // 나머지

    if (k )
    {
        /* code */
    }
    

    return 0;
}