#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    cin >> n >> m;

    vector<int> bsk(n);
    for (size_t i = 0; i < n; i++)
    {
        bsk[i] = i + 1;
    }

    vector<vector<int>> nums;

    return 0;
}