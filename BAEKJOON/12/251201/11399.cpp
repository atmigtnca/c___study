#include <iostream>
using namespace std;
using ll = long long;
int bnk[1001];

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    int N;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        cin >> bnk[i];
    }

    return 0;
}