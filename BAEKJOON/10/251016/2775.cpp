#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    vector<vector<int>> tc;

    int tk, maxK = 0;
    cin >> tk;

    for (size_t i = 0; i < tk; i++)
    {
        int n, k;
        cin >> n >> k;
    }

    return 0;
}